<UserControl x:Class="Trynex.Launcher.Views.DownloadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="30,20,30,34">
            <Grid Margin="2,0,2,22">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Downloads.Title}" FontSize="25" FontWeight="SemiBold" />
                    <TextBlock Text="{DynamicResource Downloads.Subtitle}"
                               Margin="0,5,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>
                <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#111723"
                        BorderBrush="#273044" BorderThickness="1" CornerRadius="10" Padding="12,7">
                    <TextBlock Text="{DynamicResource Downloads.Active}" FontSize="9" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" />
                </Border>
            </Grid>

            <Border Height="300" Style="{StaticResource PageCardStyle}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid Width="74" Height="74">
                        <Ellipse Fill="#121A29" Stroke="#25324A" StrokeThickness="1" />
                        <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="27"
                                   Foreground="{StaticResource CyanBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                    <TextBlock Text="{DynamicResource Downloads.Channel.Title}" Margin="0,18,0,0" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="{DynamicResource Downloads.Channel.Description}"
                               Margin="0,8,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>
            </Border>

            <Grid Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,8,0" Padding="20" Style="{StaticResource PageCardStyle}">
                    <StackPanel>
                        <Border Width="38" Height="38" HorizontalAlignment="Left" Background="#17132C" CornerRadius="10">
                            <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="#A985FF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{DynamicResource Downloads.Manifest.Title}" Margin="0,14,0,0" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource Downloads.Manifest.Description}"
                                   Margin="0,7,0,0" FontSize="10" LineHeight="16" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="4,0" Padding="20" Style="{StaticResource PageCardStyle}">
                    <StackPanel>
                        <Border Width="38" Height="38" HorizontalAlignment="Left" Background="#0D2820" CornerRadius="10">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="{StaticResource GreenBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{DynamicResource Downloads.Resume.Title}" Margin="0,14,0,0" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource Downloads.Resume.Description}"
                                   Margin="0,7,0,0" FontSize="10" LineHeight="16" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Margin="8,0,0,0" Padding="20" Style="{StaticResource PageCardStyle}">
                    <StackPanel>
                        <Border Width="38" Height="38" HorizontalAlignment="Left" Background="#10202D" CornerRadius="10">
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="{StaticResource CyanBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{DynamicResource Downloads.Rollback.Title}" Margin="0,14,0,0" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource Downloads.Rollback.Description}"
                                   Margin="0,7,0,0" FontSize="10" LineHeight="16" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Margin="0,18,0,0" Padding="18" Background="#17140E" BorderBrush="#42361A"
                    BorderThickness="1" CornerRadius="12">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Downloads.Security.Title}" FontSize="11" FontWeight="SemiBold" Foreground="#75F091" />
                        <TextBlock Text="{DynamicResource Downloads.Security.Description}"
                                   Margin="0,5,0,0" FontSize="10" Foreground="#B9A98B" />
                    </StackPanel>
                    <TextBlock Text="{DynamicResource Downloads.FoundationReady}" HorizontalAlignment="Right" VerticalAlignment="Center"
                               FontSize="8" FontWeight="Bold" Foreground="#FFB547" />
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
